# qr.rs
QR Code Generator that creates QR codes for URLs, text, and contact info.

## Usage

Run the CLI tool using `cargo run`:

```bash
cargo run -- --text "Hello World" --output qr.png
```

This will generate a QR code image named `qr.png` with the content "Hello World".
